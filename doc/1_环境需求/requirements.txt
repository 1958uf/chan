# ============================================================
# Chan 缠论框架 - 依赖包
# ============================================================
# 安装方式：
#   pip install -r requirements.txt
#
# 如需安装可选依赖（GUI / 数字货币 / 机器学习）：
#   pip install -r requirements.txt -r requirements-optional.txt
# ============================================================

# ---------- 核心依赖（必装） ----------
baostock>=0.9.2          # A股数据源（默认）
matplotlib>=3.7.0        # 图表绘制
pandas>=1.5.0            # 数据处理
ipython>=7.0.0           # 动态图表支持（AnimatePlotDriver）
typing_extensions>=4.5.0 # Python < 3.11 的 Self 类型兼容

# ---------- 可选数据源 ----------
akshare>=1.10.0          # A股备用数据源（需要能访问东方财富接口）
